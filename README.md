
# Online Leave Application Dashboard

React + Vite front end for leave applications and role-based dashboards.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
